spring:
  application:
    name: AuthMicroservice
  datasource:
    url: jdbc:postgresql://localhost:5432/IHW
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate.ddl-auto: update
    properties.hibernate:
      show_sql: true
      format_sql: true